import { revalidateTag } from 'next/cache';export async function GET(req){const url=new URL(req.url);const token=url.searchParams.get('token');if(!process.env.REFRESH_TOKEN||token!==process.env.REFRESH_TOKEN){return new Response(JSON.stringify({error:'Unauthorized'}),{status:401});}revalidateTag('deals');return new Response(JSON.stringify({ok:true,revalidated:true,ts:Date.now()}));}